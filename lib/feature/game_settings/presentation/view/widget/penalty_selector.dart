import 'package:flutter/material.dart';

class PenaltySelector extends StatelessWidget {
  const PenaltySelector({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
