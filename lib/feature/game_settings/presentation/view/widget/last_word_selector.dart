import 'package:flutter/material.dart';

class LastWordSelector extends StatelessWidget {
  const LastWordSelector({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
